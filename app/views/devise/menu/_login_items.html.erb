<% if user_signed_in? %>
  <%= link_to('Logout', destroy_user_session_path, :method => :delete), redirect_to: welcome_index_path %>        
<% else %>
  <li>
  <%= link_to('Login', new_user_session_path)  %>  
  </li>
<% end %>